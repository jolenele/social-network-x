# Copy this file to .env.local and fill in real values

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Development redirect (must match the value you register in Google Cloud Console)
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# Google API Key - used for Vision API and Vertex AI Gemini models via Google Cloud Console
GOOGLE_API_KEY=your-google-api-key

# Optional: public base url used by client-side code (if needed)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database (example)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Production examples (uncomment and edit when deploying)
# GOOGLE_REDIRECT_URI=https://your-production-domain.com/api/auth/google/callback
# NEXT_PUBLIC_BASE_URL=https://your-production-domain.com

# Notes:
# - The redirect URI must match exactly what you add under "Authorized redirect URIs" in
#   Google Cloud Console for your OAuth 2.0 Client ID (scheme, host, port, path, and trailing slash).
# - After editing, restart your Next.js dev server so environment variables are picked up.
